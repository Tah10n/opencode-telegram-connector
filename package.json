{
  "name": "telegram-opencode-connector",
  "description": "Telegram connector for opencode with concurrent per-thread bindings across multiple projects.",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "telegram-opencode-connector": "./src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "check": "node scripts/check-syntax.mjs",
    "lint": "npm run check",
    "test": "node --test"
  }
}
